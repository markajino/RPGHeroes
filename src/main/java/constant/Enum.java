package constant;

public class Enum{

//make roles and deine values in it



    public enum Roles_Enum {
        Mage,
        Ranger,
        Rogue,
        Warrior,

    }

// make weapon type enum class and defined values

    public enum Weapon_Type_Enum {
        Axes,
        Bows,
        Dagger,
        Hammer,
        Staff,
        Sword,
        Wand,
    }

    // make slot enum and define it values
    public enum Slot_Enum {
        Head,
        Body,
        Legs,
        Weapon,



    }

    //make armor type enum and defined values in it
    public enum Armor_Type_Enum {
        Cloth,
        Leather,
        Mail,
        Plate,
    }


}
